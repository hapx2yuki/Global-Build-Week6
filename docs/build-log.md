# 開発記録

Codex、GPT-5.6、人間の寄与を、後から具体的に説明できる粒度で記録する。プロンプト全文や秘密情報は残さず、目的、出力、採否、検証結果を記録する。

| 日時 | 対象 | Codex の寄与 | GPT-5.6 の寄与 | 人間の判断 | 検証・成果物 | Session ID |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-07-20 | 提出運用基盤 | 要件の照合、文書・検査・継続的検査の初期構築 | Codex 内で要件整理と構築に使用 | 非公開リポジトリから開始し、製品技術を先に固定しない方針 | `make preflight`、初回コミット | `/feedback` 後に記録 |
| 2026-07-21 | CriteriaForge 画面試作 | shadcn/ui 公式体系の対応付け、Next.js画面実装、状態遷移、デスクトップ・スマートフォンの実ブラウザ検証 | 長い設計対話から、文書中心の情報構造、出所表示、作成可能性、意図と現実の差分表示へ具体化 | 製品約束、8区分、5条件、絶対評価、情報分離、視覚方向、shadcn/ui準拠を承認 | `apps/web`、`docs/screen-design.md`、`docs/ui-component-strategy.md`、型検査・静的検査・本番ビルド、`output/playwright/` | `/feedback` 後に記録 |
| 2026-07-21 | 本番中核 | JSON Schema、SQLite、形式別根拠処理、OAuth再利用、3回評価、根拠検証、不可逆版、共有一式、一時worktree修正、公開版分離構築を実装 | `gpt-5.6-sol`でFounderBriefを同一条件3回評価し、必須2件の失敗と品質2件を安定集約 | AI出力の文字列一致ではなく重要判定と根拠内容の一致を安定性とすること、私的引用を共有しないことを維持 | 57件の単体・統合試験、4件の画面試験、公開版分離検査、依存物高・重大0件 | `/feedback` 後に記録 |
| 2026-07-21 | 公開 | GitHub公開、Pull Request、Vercel本番配備、公開URLへの画面検査を実施 | 使用なし | Vercelの実ログを根拠にNode.js 20から24へ固定版を変更 | GitHub公開、`https://criteriaforge.vercel.app`、公開URL上の4画面試験成功 | `/feedback` 後に記録 |
| 2026-07-21 | ローカル実経路 | 7画面を永続状態へ接続し、送信確認、校正、Git固定、正式評価、修正境界、前後比較を実装。構造化出力の`uniqueItems`互換性と引用ID捏造を実測から修正 | `gpt-5.6-terra`で憲法案1回、校正3回、正式評価3回を実行 | 全規則と例を明示承認し、重要質問2件に回答。不一致評価を修正へ流さず停止する権限境界を維持 | 65件の単体・統合試験。憲法v1.0作成、314区分のGit固定、正式評価`unstable / blocked`、再起動復元 | `/feedback` 後に記録 |

## 2026-07-21 GPT‑5.6実測

- 最初の実行群では、ある実行が「根拠へ到達できない」と解釈し不安定になった。送信前に端末内で検証済みの抜粋であり、返却後にも再検証する境界を指示へ明記した。
- 次の実行群では品質項目の`mustPass`表現が`pass`と`not_applicable`に揺れた。品質項目は品質段階だけで表し、必須判定を`not_applicable`へ決定論的に正規化した。
- 最終実行群は構造修復0回で`stable / not_met`に集約した。出力ハッシュは3回とも異なるため、完全決定性を主張していない。

## 2026-07-21 画面検証で見つけて直した点

- 分割領域の比率を数値で渡したため画素として解釈され、質問パネルが画面外へ押し出された。比率を文字列で指定して解消した。
- `CommandDialog` 内に `Command` の文脈がなく、`⌘K` で入力部品が失敗した。公式の構成に合わせて明示的に組み合わせた。
- スマートフォンでタブと質問ボタンの表示文字が隠れ、読み上げ名が空になった。幅に依存しない `aria-label` を追加した。
- スマートフォンの作成可能性5条件が縦に並び、本文を覆いすぎた。小画面では名称を読み上げ名に残し、印を横一列にした。
- 最終確認では、390×844の主要経路と側面ナビゲーションを操作でき、ブラウザのエラー0件・警告0件だった。

## 記録する内容

- 何を速く、正確に、または新しく実現できたか。
- 生成結果をそのまま採用せず、何を人間が判断したか。
- 失敗した案、修正した理由、最終結果の確認方法。
- GPT-5.6 でなければ難しかった長文理解、複数段階推論、道具利用など。
